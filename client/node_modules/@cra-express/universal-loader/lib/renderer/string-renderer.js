"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = stringRenderer;

var _server = require("react-dom/server");

function stringRenderer(req, res, reactEl, htmlData, options) {
  var str = (0, _server.renderToString)(reactEl);
  var segments = htmlData.split("<div id=\"root\">");

  if (options.onEndReplace) {
    segments[1] = options.onEndReplace(segments[1]);
  }

  var finalStr = "".concat(segments[0], "<div id=\"root\">").concat(str).concat(segments[1]);

  if (options.onFinish) {
    options.onFinish(req, res, finalStr);
    return;
  }

  res.send(finalStr);
}